on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
  steps:
      - name: 'Before'
        run: ls

      - name: Checkout repository
        uses: actions/checkout@v3

      - name: 'After'
        run: ls